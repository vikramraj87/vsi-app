(function(angular){
    angular.module('auth')
        .factory('authService', ['authHttpFacade', 'sessionFactory', function(authHttpFacade, sessionFactory) {
            var rules = [];

            return {

            };
        }]);
}(angular));
